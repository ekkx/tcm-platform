// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: create_subscription.sql

package sqlc

import (
	"context"
	"time"

	"github.com/ekkx/tcm-platform/internal/platform/ulid"
)

const createSubscription = `-- name: CreateSubscription :one
INSERT INTO
    subscriptions (
        id,
        user_id,
        stripe_customer_id,
        stripe_subscription_id,
        stripe_price_id,
        plan,
        monthly_hours,
        status,
        current_period_start,
        current_period_end,
        create_time,
        update_time
    )
VALUES
    (
        $1::ulid,
        $2::ulid,
        $3::TEXT,
        $4::TEXT,
        $5::TEXT,
        $6::plan_type,
        $7::INT,
        $8::TEXT,
        $9::TIMESTAMPTZ,
        $10::TIMESTAMPTZ,
        NOW(),
        NOW()
    )
RETURNING subscriptions.id
`

type CreateSubscriptionParams struct {
	ID                   ulid.ULID  `json:"id"`
	UserID               ulid.ULID  `json:"user_id"`
	StripeCustomerID     *string    `json:"stripe_customer_id"`
	StripeSubscriptionID *string    `json:"stripe_subscription_id"`
	StripePriceID        *string    `json:"stripe_price_id"`
	Plan                 PlanType   `json:"plan"`
	MonthlyHours         *int32     `json:"monthly_hours"`
	Status               string     `json:"status"`
	CurrentPeriodStart   *time.Time `json:"current_period_start"`
	CurrentPeriodEnd     *time.Time `json:"current_period_end"`
}

func (q *Queries) CreateSubscription(ctx context.Context, arg CreateSubscriptionParams) (ulid.ULID, error) {
	row := q.db.QueryRow(ctx, createSubscription,
		arg.ID,
		arg.UserID,
		arg.StripeCustomerID,
		arg.StripeSubscriptionID,
		arg.StripePriceID,
		arg.Plan,
		arg.MonthlyHours,
		arg.Status,
		arg.CurrentPeriodStart,
		arg.CurrentPeriodEnd,
	)
	var id ulid.ULID
	err := row.Scan(&id)
	return id, err
}
